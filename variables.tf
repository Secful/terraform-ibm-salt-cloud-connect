# This module takes no customer-tunable inputs. All resource names are
# namespaced by an auto-generated stack_id (random_id). An orchestrating
# script (ibm/onboarding/cloud-shell-onboard.sh in the parent repo)
# reads the outputs and posts them to the Salt Security backend.